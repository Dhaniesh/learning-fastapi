name: GitHub Actions Demo
on:
    pull_request:
    push:
jobs:
    explore-github-actions:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Deploying to development sever $GITHUB_REF"